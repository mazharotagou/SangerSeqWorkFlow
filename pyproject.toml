[project]
name = "sangerseqworkflow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mazharotagou", email = "mazharotagou@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "python-fasthtml>=0.14.13",
]

[project.scripts]
sangerseqworkflow = "sangerseqworkflow:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
